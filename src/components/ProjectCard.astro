---
interface Props {
  title: string;
  description: string;
  metrics: string;
  tech: string;
  anchor: string;
}

const { title, description, metrics, tech, anchor } = Astro.props;
const techArray = tech.split(' · ');
---

<div class="project-card" id={anchor}>
  <h3 class="title">{title}</h3>
  <p class="description">{description}</p>
  <div class="metrics">
    <p>{metrics}</p>
  </div>
  <div class="tech-chips">
    {techArray.map((t) => <span class="chip">{t}</span>)}
  </div>
  <a href={`/projects#${anchor}`} class="link">View project →</a>
</div>

<style>
  .project-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 2rem;
    transition: all 0.3s;
  }

  .project-card:hover {
    background: var(--card-highlight);
    border-color: var(--border-accent);
  }

  .title {
    color: var(--text);
    margin: 0 0 1rem 0;
    font-size: 1.2rem;
  }

  .description {
    color: var(--text-body);
    font-size: 0.95rem;
    line-height: 1.6;
    margin: 0 0 1rem 0;
  }

  .metrics {
    color: var(--text-muted);
    font-size: 0.85rem;
    margin: 1rem 0;
    line-height: 1.6;
  }

  .metrics p {
    margin: 0;
  }

  .tech-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin: 1rem 0;
  }

  .chip {
    background: var(--chip-bg);
    color: var(--accent);
    border: 1px solid var(--chip-border);
    border-radius: 4px;
    padding: 0.25rem 0.75rem;
    font-size: 0.8rem;
  }

  .link {
    display: inline-block;
    color: var(--accent);
    text-decoration: none;
    margin-top: 1rem;
    transition: color 0.2s;
  }

  .link:hover {
    color: var(--accent-2);
  }
</style>
